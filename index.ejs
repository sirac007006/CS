<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CS2 Float Trading</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        :root {
            --primary: #ffd700;
            --primary-dark: #ccac00;
            --bg-dark: #121212;
            --bg-card: #1e1e1e;
            --text-primary: #ffffff;
            --text-secondary: #9ca3af;
            --border-color: #2a2a2a;
        }

        body {
            background-color: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
        }

        .top-nav {
            background-color: var(--bg-card);
            padding: 0.75rem 1.5rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 100;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .nav-links a {
            color: var(--text-primary);
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.2s;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .nav-links a.active {
            color: var(--primary);
        }

        .logo {
            color: var(--primary);
            font-size: 1.5rem;
            font-weight: bold;
            text-decoration: none;
            margin-right: 2rem;
        }

        .top-nav-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .refresh-button {
            background-color: var(--primary);
            color: var(--bg-dark);
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 500;
            transition: background-color 0.2s;
        }

        .refresh-button:hover {
            background-color: var(--primary-dark);
        }

        .balance {
            background-color: var(--bg-dark);
            padding: 0.5rem 1rem;
            border-radius: 4px;
            color: var(--primary);
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .category-nav {
            position: fixed;
            top: 60px;
            left: 0;
            width: 100%;
            background-color: var(--bg-card);
            padding: 0.75rem 1.5rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 99;
        }

        .nav-left {
            display: flex;
            gap: 1rem;
        }

        .sort-select {
            background-color: var(--bg-dark);
            color: var(--text-primary);
            padding: 0.5rem;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            cursor: pointer;
        }

        .sort-select option {
            background-color: var(--bg-dark);
            color: var(--text-primary);
        }

        .main-content {
            margin-top: 120px;
            padding: 1rem;
            display: flex;
            gap: 1rem;
        }

        .filters-sidebar {
            width: 280px;
            background-color: var(--bg-card);
            border-radius: 8px;
            padding: 1rem;
            height: calc(100vh - 140px);
            position: fixed;
            overflow-y: auto;
        }

        .items-grid {
            margin-left: 300px;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1rem;
            width: calc(100% - 320px);
        }

        .filter-section {
            margin-bottom: 1.5rem;
        }

        .filter-title {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .search-input {
            width: 100%;
            padding: 0.75rem;
            background-color: var(--bg-dark);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-primary);
        }

        .wear-slider {
            width: 100%;
            height: 4px;
            background: linear-gradient(to right, #4ade80, #fbbf24, #ef4444);
            border-radius: 2px;
            margin: 1rem 0;
        }

        .wear-inputs {
            display: flex;
            gap: 0.5rem;
        }

        .wear-input {
            width: 50%;
            padding: 0.5rem;
            background-color: var(--bg-dark);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-primary);
        }

        .wear-badges {
            display: flex;
            gap: 0.25rem;
            margin-top: 0.5rem;
            flex-wrap: wrap;
        }

        .wear-badge {
            padding: 0.25rem 0.5rem;
            background-color: var(--bg-dark);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            font-size: 0.8rem;
            cursor: pointer;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .wear-badge.active {
            background-color: var(--primary);
            border-color: var(--primary);
            color: var(--bg-dark);
        }

        .collection-select {
            width: 100%;
            padding: 0.5rem;
            background-color: var(--bg-dark);
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-primary);
            cursor: pointer;
            margin-bottom: 1rem;
        }

        .collection-select option {
            background-color: var(--bg-dark);
            color: var(--text-primary);
        }

        .item-card {
            background-color: var(--bg-card);
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.2s;
            border: 1px solid var(--border-color);
        }

        .item-card:hover {
            transform: translateY(-4px);
            border-color: var(--primary);
        }

        .item-image {
            width: 100%;
            height: 200px;
            background-color: var(--bg-dark);
            position: relative;
        }

        .item-details {
            padding: 1rem;
        }

        .item-name {
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        .float-value {
            font-size: 0.85rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .price {
            color: var(--primary);
            font-size: 1.1rem;
            font-weight: bold;
        }

        .float-bar {
            height: 4px;
            background: linear-gradient(to right, #4ade80, #fbbf24, #ef4444);
            position: relative;
            margin: 0.5rem 0;
        }

        .float-indicator {
            position: absolute;
            width: 8px;
            height: 8px;
            background: white;
            border-radius: 50%;
            top: -2px;
            transform: translateX(-50%);
        }

        @media (max-width: 1024px) {
            .filters-sidebar {
                display: none;
            }
            
            .items-grid {
                margin-left: 0;
                width: 100%;
            }
        }

        .type-filter {
            margin-top: 1rem;
        }

        .type-badges {
            display: flex;
            gap: 0.25rem;
            margin-top: 0.5rem;
            flex-wrap: wrap;
        }
    </style>
</head>
<body>
    <nav class="top-nav">
        <div class="nav-links">
            <a href="/" class="logo">FLOAT MARKET</a>
            <a href="/market" class="active">Market</a>
            <a href="/database">Database</a>
        </div>
        <div class="top-nav-right">
            <button class="refresh-button" onclick="refreshResults()">
                Refresh Results
            </button>
            <div class="balance">1337.00 €</div>
            <div class="user-menu">
                <span>Cart (0)</span>
                <span>Profile</span>
            </div>
        </div>
    </nav>

    <div class="category-nav">
        <div class="nav-left">
            <button class="wear-badge active">All Items</button>
            <button class="wear-badge">Special Items</button>
            <button class="wear-badge">Sticker Crafts</button>
        </div>
        <select class="sort-select">
            <option value="best_deals">Best Deals</option>
            <option value="lowest_float">Lowest Float</option>
            <option value="highest_float">Highest Float</option>
            <option value="lowest_price">Lowest Price</option>
            <option value="highest_price">Highest Price</option>
        </select>
    </div>

    <div class="main-content">
        <div class="filters-sidebar">
            <div class="filter-section">
                <div class="filter-title">Search</div>
                <input type="text" class="search-input" placeholder="Search items...">
            </div>

            <div class="filter-section">
                <div class="filter-title">Float Range</div>
                <div class="wear-slider"></div>
                <div class="wear-inputs">
                    <input type="text" class="wear-input" placeholder="Min">
                    <input type="text" class="wear-input" placeholder="Max">
                </div>
                <div class="wear-badges">
                    <span class="wear-badge">FN</span>
                    <span class="wear-badge active">MW</span>
                    <span class="wear-badge">FT</span>
                    <span class="wear-badge">WW</span>
                    <span class="wear-badge">BS</span>
                </div>
            </div>

            <div class="filter-section">
                <div class="filter-title">Price Range</div>
                <div class="wear-inputs">
                    <input type="text" class="wear-input" placeholder="Min €">
                    <input type="text" class="wear-input" placeholder="Max €">
                </div>
            </div>

            <div class="filter-section">
                <div class="filter-title">Collection</div>
                <select class="collection-select">
                    <option value="">All Collections</option>
                    <option value="revolution">Revolution Case</option>
                    <option value="recoil">Recoil Case</option>
                    <option value="dreams">Dreams & Nightmares Case</option>
                    <option value="2022">2022 Train Collection</option>
                    <option value="2021">2021 Dust 2 Collection</option>
                    <option value="2021">2021 Mirage Collection</option>
                    <option value="2021">2021 Vertigo Collection</option>
                    <option value="2021">2021 Train Collection</option>
                    <option value="anubis">Anubis Collection</option>
                    <option value="ancient">Ancient Collection</option>
                    <option value="havoc">Havoc Collection</option>
                    <option value="control">Control Collection</option>
                    <option value="snake">Snake Collection</option>
                    <option value="canals">Canals Collection</option>
                    <option value="cobble">Cobblestone Collection</option>
                    <option value="inferno">2018 Inferno Collection</option>
                    <option value="norse">Norse Collection</option>
                    <option value="nuke">2018 Nuke Collection</option>
                    <option value="gods">Gods and Monsters Collection</option>
                    <option value="chop">Chop Shop Collection</option>
                    <option value="rising">Rising Sun Collection</option>
                    <option value="baggage">Baggage Collection</option>
                    <option value="overpass">Overpass Collection</option>
                    <option value="cache">Cache Collection</option>
                    <option value="stmarc">St. Marc Collection</option>
                    <option value="italy">Italy Collection</option>
                    <option value="lake">Lake Collection</option>
                    <option value="safehouse">Safehouse Collection</option>
                    <option value="train">Train Collection</option>
                    <option value="militia">Militia Collection</option>
                    <option value="assault">Assault Collection</option>
                    <option value="office">Office Collection</option>
                    <option value="dust">Dust Collection</option>
                    <option value="aztec">Aztec Collection</option>
                    <option value="vertigo">Vertigo Collection</option>
                    <option value="bank">Bank Collection</option>
                </select>

                <div class="filter-title">Item Type</div>
                <div class="type-badges">
                    <span class="wear-badge active">Normal</span>
                    <span class="wear-badge">StatTrak™</span>
                    <span class="wear-badge">Souvenir</span>
                </div>
            </div>
        </div>

        <div class="items-grid">
            <div class="item-card">
                <div class="item-image"></div>
                <div class="item-details">
                    <div class="item-name">AWP | Dragon Lore</div>
                    <div class="float-value">Float: 0.0182991884</div>
                    <div class="float-bar">
                        <div class="float-indicator" style="left: 18%"></div>
                    </div>
                    <div class="price">11,374.70 €</div>
                </div>